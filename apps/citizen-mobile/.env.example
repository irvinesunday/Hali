# Hali Citizen Mobile — Environment Configuration
# Copy to .env for local development. Never commit .env to version control.

# API base URL — point at the running .NET backend
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080

# Country code for phone number formatting
EXPO_PUBLIC_DEFAULT_COUNTRY_CODE=KE
